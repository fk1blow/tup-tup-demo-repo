name: my-pipeline
jobs:
  - name: test
    image: oven/bun
    commands:
      - ["bun", "install"]
      - ["bun", "test"]
  - name: test
    image: oven/bun
    commands:
      - ["bun", "install"]
      - ["bun", "build", "--outfile=bundle.js", "index.ts"]
